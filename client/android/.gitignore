gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Built didsa_slvs_ffi native library - regenerated by the two-step CMake
# recipe in client/native/slvs/CMakeLists.txt (Android arm64-v8a target),
# not committed as a binary since it's fully reproducible from the already-
# vendored source. Must be built and placed here before `flutter build apk`
# actually bundles local-solver support - see that CMakeLists.txt's own
# header comment for the exact build commands.
app/src/main/jniLibs/

