# CMake build output - regenerated by the two-step vcpkg configure/build
# recipe in CMakeLists.txt, never committed. Named per target (host
# desktop, Windows, Linux, Android, iOS...) - same convention
# client/native/slvs/.gitignore already uses for its own build.
build-*/

desktop_parity_harness/.dart_tool/
